# Code contracts

JSON Schema:

- [`code_change.schema.json`](code_change.schema.json) — `code.change` / PR diff payload (§5.4)

Example: [`examples/valid_code_change.json`](examples/valid_code_change.json)

Scanner rule packs used by `code-processor` live under `scanners/semgrep/` and `scanners/codeql/`.
