[project]
name = "ids-processor"
version = "0.1.0"
description = "Normalize Suricata EVE alerts from suricata.raw into findings.network"
requires-python = ">=3.12"
dependencies = [
  "black-onyx-contracts",
  "fastapi>=0.115",
  "uvicorn[standard]>=0.32",
  "pydantic-settings>=2.6",
  "kafka-python>=2.0.2",
  "python-ulid>=3.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
ids-processor = "ids_processor.main:run"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ids_processor"]

[tool.uv.sources]
black-onyx-contracts = { path = "../../packages/black_onyx_contracts" }

[tool.pytest.ini_options]
testpaths = ["tests"]
